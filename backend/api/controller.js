import fs from 'fs'



export function logger (req, res) {
      const { geoLocation } = req.body
      // let data = req.body;
      // console.log(req.body)
      // fs.appendFile('./logs/test.txt', JSON.stringify(data) + '\n', function (err) {
      //       if (err) throw err;
      //       console.log('Saved!');
      // });
      // res.send('Data Received: ' + JSON.stringify(data));
}
